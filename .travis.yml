language: java

addons:
  chrome: stable

env:
  - REPORT="allure"
  - REPORT="cucumber"

install: ./gradlew clean testClasses

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3

script: ./gradlew clean test -Preport=$REPORT