apply plugin: 'io.qameta.allure'
apply from: "${rootDir}/gradle/dependencies.gradle"

allure {
    version = '2.4.1'
    aspectjweaver = true

    useCucumberJVM {
        version = allure_version
    }
}